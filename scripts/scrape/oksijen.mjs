// Gazete Oksijen scraper — PLACEHOLDER.
// The site is a fully client-rendered SPA with no accessible sitemap or API.
// Needs Playwright / headless browser to render article listings.
// Deferred until we have Playwright infra.
console.log('[oksijen] scraper deferred — needs Playwright');
process.exit(0);
